/* ============================================================
   LangLab — Đề thi thử ТРКИ (tự soạn theo cấu trúc, không phải đề chính thức)
   Cùng khuôn với HSK_EXAMS (xem js/hsk-exams.js), thêm lang:'ru'.
   ============================================================ */
const TRKI_EXAMS = [];
if (typeof module !== 'undefined') module.exports = { TRKI_EXAMS };
